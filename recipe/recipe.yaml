schema_version: 1

context:
  version: "2.11.0"
  python_min: "3.10"

recipe:
  name: hist-suite
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/h/hist/hist-${{ version }}.tar.gz
  sha256: 92eb1fa0ca39bad619f7f704b3ba93c44b3553b0445f6db2c17c67c01b9cb534

build:
  number: 0

outputs:
  - package:
      name: hist-base

    build:
      noarch: python
      script: ${{ PYTHON }} -m pip install . -vv
      python:
        entry_points:
          - hist=hist.classichist:main

    requirements:
      host:
        - python ${{ python_min }}.*
        - pip
        - hatchling
        - hatch-vcs
      run:
        - python >=${{ python_min }}
        - boost-histogram >=1.7,<1.9
        - histoprint >=2.2
        - numpy >=1.21.3
        - packaging >=23.0
        # Actually only required for [py<311] but require for all Python versions so this can be noarch: python
        - typing-extensions >=4

    tests:
      - python:
          imports:
            - hist
            - hist.axis
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - "*"

  - package:
      name: hist

    build:
      noarch: python

    requirements:
      # Satisfy linter but not used
      host:
        - python ${{ python_min }}.*
      run:
        - ${{ pin_subpackage('hist-base', exact=True) }}
        # The plot requirements
        - matplotlib-base >=3.10.7
        - mplhep >=0.3.33
        # The fit plot requirements
        - scipy >=1.7
        - iminuit >=2.9
        # Satisfy linter but not used
        - python >=${{ python_min }}

    tests:
      - python:
          imports:
            - hist
            - hist.axis
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - "*"

about:
  summary: Hist classes and utilities
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://github.com/scikit-hep/hist
  repository: https://github.com/scikit-hep/hist
  documentation: https://hist.readthedocs.io/

extra:
  feedstock-name: hist
  recipe-maintainers:
    - conda-forge/scikit-hep
    - LovelyBuggies
